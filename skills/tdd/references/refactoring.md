# GREEN 后重构

只在聚焦测试为 GREEN 时选择小步重构：

- 合并重复；
- 改善命名和错误表达；
- 拆分难读的长逻辑为私有辅助；
- 把逻辑移动到拥有数据与不变量的位置；
- 合并浅模块或加深模块；
- 引入能消除无效状态的值对象；
- 清理本轮实现暴露出的既有问题，但不扩大到无关改写。

每一步后重跑同一聚焦命令。若行为测试因纯内部重构失败，先判断测试是否耦合实现；不要为了
保留脆弱测试而冻结内部结构。重构结束后再运行受影响测试集和最终 `coverageCheck`。
