# 深模块

优先“小接口、深实现”：让少量稳定入口隐藏验证、状态转换和基础设施细节。

评估接口时询问：

- 能否减少公共方法和参数？
- 能否把多步约束收进一个有业务含义的操作？
- 调用者是否必须理解本可由模块隐藏的内部顺序？
- 新抽象是否真的降低调用者负担，还是只增加一层转发？

避免“大接口、薄实现”和只有一个调用方的推测性抽象。测试应守住小接口表现出的行为，而不是
穿透模块验证内部步骤。
