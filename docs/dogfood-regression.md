---
title: 引擎 dogfood 回归断言清单
status: active
updated: 2026-08-20
scope: root
---

# 引擎 dogfood 回归断言清单

<!-- 历次 prompt 行为级翻车的固化断言。每轮引擎真实跑 dogfood 时逐条复查，防旧病复发；
     新的 prompt 行为翻车修复后在此追加一条。只收 prompt 行为级断言——代码级缺陷由
     Vitest 回归覆盖，不重复登记。思想来源：skill 修订应有测试用例可复测
     （agentskills.io evaluating-skills 的轻量适配：不做 with/without 对照，只做历史断言复查）。 -->

复查方式：引擎循环跑完后，按「验证点」列逐条核对工件与记录；任一条不成立即为回归，修复并复验后才能收口。

| #   | 断言                                                                                                                                                                                                                                                                                                                                                                                                                           | 来源                                                                                     | 验证点                                                                                                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 含 UI 验收的每个 story，builder 都先跑 `which agent-browser` 探测再做浏览器验证，无静默降级为 HTTP 冒烟                                                                                                                                                                                                                                                                                                                        | 0.12.1 条件句翻车                                                                        | UI story 的 builder 输出含真实浏览器验证记录（操作序列/截图对账）；无以 HTTP 冒烟等价替代的表述（--print 模式无过程 transcript，2026-07-07 起以输出与工件为核对面）                                                                         |
| 2   | 最终浏览器验证留下 `builder-[story-id]-[序号].png` 于 workspace `screenshots/`；无截图视为未验证                                                                                                                                                                                                                                                                                                                               | 0.14.1                                                                                   | 按 story 对账 screenshots 目录                                                                                                                                                                                                              |
| 3   | validator 验收执行了真实操作并留截图工件（通过与打回两分支皆有）                                                                                                                                                                                                                                                                                                                                                               | 0.3.0 / 0.12.1 反共谋实证                                                                | 同上，含打回场景                                                                                                                                                                                                                            |
| 4   | `workspace apply-prd` 以 `replace-feature` 应用新 PRD 时，固定动作归档旧运行并安装“无 state”候选（story id 重编不撞旧状态）                                                                                                                                                                                                                                                                                                    | 0.10.0 空转翻车 / ADR-021                                                                | 对账归档完整、应用后 `state.json` 不存在；引擎首轮无「旧轮 passes 误判已完成」                                                                                                                                                              |
| 5   | builder 在有意识简化处留 `// 取舍: <当前上限>，<升级触发条件>`；compound-docs 收账产出账本（非空或「无取舍债务」）                                                                                                                                                                                                                                                                                                             | 0.8.0 约定 / 0.12.1 首次触发                                                             | `grep -rn "取舍:"` 与收口账本对照                                                                                                                                                                                                           |
| 6   | 打回链路中 notes 保全仲裁标签行（`[需求冲突]`、`[需要人工核实]`）不丢失                                                                                                                                                                                                                                                                                                                                                        | 0.4.0 仲裁约定 / 0.18.1 扩前缀族                                                         | 触发过打回的 story 检查 notes                                                                                                                                                                                                               |
| 7   | 质量契约与固定版本匹配、PRD 摘要和派生检查快照完全一致时，builder 后 validator 前按固定类别执行；结构化命令不经 shell，失败确定性打回且该轮跳过 validator；任一绑定不一致都在 agent 启动前退出 2                                                                                                                                                                                                                               | ADR-018                                                                                  | 分别制造摘要漂移、快照删减、版本不符与一项检查失败，对账零 agent 调用或门禁打回路径                                                                                                                                                         |
| 8   | 最终 Spec Review 必须反向核对每项实际改动是否属于 PR 目标/AC，并把违反明确非目标或做多的行为独立报告；`/review-loop` 只裁决这些结构化 findings                                                                                                                                                                                                                                                                                 | 0.13.0 / ADR-018                                                                         | 检查 Spec 轴输入与 findings；注入 AC 外行为，确认不会被工程轴或汇总结论抵消                                                                                                                                                                 |
| 9   | /compound-docs 沉淀中改写/删除既有条目时，交付说明附规则变更清单（旧表述 → 新表述 + 当前代码依据）                                                                                                                                                                                                                                                                                                                             | 0.14.4                                                                                   | 收口交付说明检查（无改写则不适用）                                                                                                                                                                                                          |
| 10  | /compound-docs 收口交付说明含「状态变更清单」节（有变更列明细，无变更写「无状态变更」），任务型文档按证据表判定收尾                                                                                                                                                                                                                                                                                                            | 0.15.0                                                                                   | 收口交付说明检查                                                                                                                                                                                                                            |
| 11  | 循环结束（完成或跑满）workspace 根自动生成验证报告 report.html：标明 PRD 来自引擎启动快照，Story 实现、本地最终 Review 与 GitHub 交付分栏显示，story 卡片截图与 screenshots/ 对账，存在 prd.tampered-* 时红旗区必亮；旧 review-*.md 只标为不可作为通过证明的历史本地反馈；state 损坏时红色“状态不可验证”且绝无通过 badge                                                                                                       | 0.19.0 / 0.25.4 / ADR-018                                                                | 打开 report.html 与工件对账；只完成 Story 时确认不显示可交付；损坏 state 后手动 report 应写诊断报告并退出 1                                                                                                                                 |
| 12  | 证据索引真实链路：evidence.jsonl 含三类 engine 记录且可重建已权威结算的轮次（每个这类轮次恰有一条 iteration，异常轮带 outcome/noop/gateRejected/abortRollback 标注可直读还原）；`operation-proof-missing` 只保留安全协议与隔离事实，不写普通 iteration；builder/validator 按指令登记 screenshot-claim（grep acIndex 分布核对输入质量：整数、1 起、无越界泛滥）；报告 AC 对账区与门禁执行历史真实渲染                                                                                   | 0.20.0（终审风险②③④固化）；0.22.0 还原链重构                                             | 真实跑后逐类 grep evidence.jsonl + 打开 report.html 对账                                                                                                                                                                                    |
| 13  | 启用按难度模型路由时，真实 builder/validator 分别命中 story 档位与 validator 映射；启动摘要、evidence、status JSON 与 report 时间线里的模型 ID 和 route source 四处一致                                                                                                                                                                                                                                                        | 0.23.0 / 0.24.0                                                                          | 对账控制台、iteration 记录、`status --json.recentActual` 与 report 时间线                                                                                                                                                                   |
| 14  | builder 原样保留 `validated`/`escalated`；Validator 不修改任何 state 字段，只提交结构化 claim；只有引擎接受 passed claim 后签发 `validated=true`，status/report 才显示全绿                                                                                                                                                                                                                                                     | 0.25.0–0.25.1 / ADR-015 收紧                                                             | builder 后候选态、Validator 前后 state、最终 state、iteration.validationReceipt、status/report 对账                                                                                                                                         |
| 15  | 使用 npm 正式发布物运行时，全局模型目录、预检与真实 runner 调用贯通；目录不做在线发现，provider 的非阻断诊断噪声不得改变引擎对进程结局和验收凭证的机械判定                                                                                                                                                                                                                                                                     | 0.24.0 / 0.25.2                                                                          | `config validate`、启动目录摘要、runner 实际结局、最终退出码与锁释放对账                                                                                                                                                                    |
| 16  | builder 的 story 提交只包含实现与测试，不包含 prd/state/progress/evidence/report/lock 等 workspace 运行时文件；即使目标仓尚未 gitignore workspace，也必须先提交业务改动，再单独回写 state/progress                                                                                                                                                                                                                             | 0.25.2 真实链路发现                                                                      | `git show --name-only` 对账 story commit，循环结束后检查 runtime diff 只留在 workspace                                                                                                                                                      |
| 17  | prd-to-json 只在项目与 workspace 外的系统临时目录生成候选和一次性请求；活动租约、无效输入或基线失败时，`workspace apply-prd` 拒绝且保持业务树零变化                                                                                                                                                                                                                                                                            | ADR-021 原子写入口                                                                       | 持活动租约分别触发 replace/rederive；对账 skill 未直写、命令失败、业务文件逐字节不变，协议记录只保留允许的拒绝事实                                                                                                                          |
| 18  | 用户显式传入的 workspace 参数从 skill 到正式命令原样保留；相对路径、绝对路径和符号链接别名都落到同一 canonical 身份并竞争同一租约，不再依赖二次 doctor 缩小竞态                                                                                                                                                                                                                                                                | 0.25.4 TOCTOU 修正 / ADR-021                                                             | 以三种拼写同时发起 apply；确认只有一个 owner，其他入口拒绝，且请求始终位于 workspace 外                                                                                                                                                     |
| 19  | Validator 只验证 engine-bound request 指定的 story/AC、固定 Story 起点、最终 Git HEAD 与变化摘要，并按 1..N 写 v2 result；缺结果、复用旧文件、错 story/hash/base/HEAD/manifest、漏 AC 都保留候选、标记不可验证并退出 5；改写 state 属于越权，隔离并退出 2；任一场景都不得签发凭证                                                                                                                                                                                              | ADR-015/023/026 可信目标与故障分层                                                       | 正常 pass/fail 各跑一次，再依次注入 missing/stale/mismatch/state-mutation；对账 `validation-claim`、iteration protocol/error、候选是否保留、`validatorUnverifiable`、status/report 和 workspace 是否隔离                                    |
| 20  | 真实 Runner 的 provider/认证/网络异常必须按发生阶段和收口证明区分：自然数字退出时，Developer 异常沿用未完成/重试语义，Validator 异常保留候选、退出 5 且无凭证；POSIX 不透明 Runner 已启动后被超时、用户中断、父进程关闭或输出通道故障从外部终止，或以 signal 结束、观察到 `process-tree-not-empty` 时，必须永久隔离且不得启动下一轮。只有已随普通 iteration 写入的权威结算调用，才由 evidence/status/report 留下结局、退出码、收口耗时与有界诊断；若 Validator proof-missing，整轮不写普通 iteration，本轮此前已结算的 Builder 也不单独持久化，proof-missing 只保留安全协议、隔离状态、终端输出和受保护现场，不计入 stall | 2026-07-22 Claude/Codex 双 runner dogfood；2026-08-09 RC2 跨组进程实证 / ADR-016/021/023 | fake 402 分别注入 Developer 与 Validator 并对账原有异常语义；再让真实 Runner 内部命令进入独立 POSIX session，先在 Builder 阶段分别触发 timeout/用户中断，确认 `operation-proof-missing`、lease 永久隔离、调用次数为 1 且无下一轮 marker；再换新 workspace 让 Builder 正常结算、Validator 分别触发 timeout/用户中断，确认整轮无普通 iteration、无独立 Builder 调用凭证、不计 stall、无下一轮 marker；两类现场都独立确认旧进程不再影响原项目目录 |
| 21  | 启用 TDD 时，builder 对每个公共行为留下可复核的真实 RED→同命令 GREEN→绿色重构记录；环境错误不能冒充 RED，过程记录不得被报告成机器证明                                                                                                                                                                                                                                                                                          | ADR-017 强化版 A                                                                         | 真实 story 对账 builder 输出/progress 与聚焦测试结局；人工抽查一个错误 RED 场景会停止而不是继续实现                                                                                                                                         |
| 22  | Codex/Claude 插件 hook 与显式安装的 Cursor 项目检查只在 agent commit 前提前运行 TDD 检查；失败阻断、成功放行，且不安装目标 Git hook、不写持久日志。Cursor 首次/升级安装幂等，卸载保留用户原配置；真实验收不得用桌面应用代替                                                                                                                                                                                                    | ADR-017 跨宿主适配                                                                       | 三种真实 payload 对账共同脚本；Codex/Claude 真实插件 smoke；用构建产物执行 `hooks cursor install/status/remove`，再由真实 Cursor Agent 验证失败时 Git 历史不变、成功时提交产生                                                              |
| 23  | 无论宿主 hook 是否触发或曾通过，引擎都在契约派生检查后、Validator 前独立校验政策摘要/基线/新增 ignore marker并运行 coverageCheck；失败打回、跳过 Validator，`tdd-gate` evidence/report 区分政策失败与覆盖命令失败                                                                                                                                                                                                              | ADR-017 最终裁决                                                                         | 绕过 hook 后制造 coverage 失败、政策文件漂移、已提交 ignore marker 各跑一轮，对账 Validator 零调用、state/证据/报告                                                                                                                         |
| 24  | 候选 coding-x 只有显式 `--shadow` 才能越过固定版本不匹配；同一绝对候选 CLI 的 doctor、apply-prd 与最终收敛健康时均固定退出 7，失败仍保留真实失败码；Story 凭证绑定实际候选版本和 formal/shadow 模式，任何 shadow 结果都不能被正式模式或另一候选复用                                                                                                                                                                            | ADR-018                                                                                  | 用契约版本 N 运行候选 N+1：普通 doctor/apply 零写失败，shadow doctor=shadow/7、apply=applied-shadow/7；再把 shadow 凭证切到 formal 和 N+2，确认保留 passes、撤销 validated 并重跑 Validator                                                 |
| 25  | Spec Review 只判断改动行为，不循环证明本轮三轴 Review、GitHub 门禁或发布已经完成；这些后置条件由引擎独立判定                                                                                                                                                                                                                                                                                                                   | 0.33.0 / ADR-018                                                                         | 在 PR 验证计划中声明本地 Review 与 GitHub 检查，确认审查包带当前 head 的机械检查事实和明确责任边界，Spec 轴不会因未来状态缺席而误报不可验证                                                                                                 |
| 26  | run、repair、report、apply-prd、review-decision 与其他正式父进程写入口共享同一 session/lease 协议；第二个 writer 不能因命令不同而并发进入                                                                                                                                                                                                                                                                                      | ADR-021 正式写入口统一                                                                   | 逐对并发启动写命令；确认首个 owner 存活期间第二个入口稳定拒绝，首个结束后才能取得新租约                                                                                                                                                     |
| 27  | `/review-loop` 只生成不含 HEAD、时间和路径的一次性最小请求；`workspace record-review-decision` 在租约内重核当前 Review/finding/Issue 并由引擎填入提交与时间绑定                                                                                                                                                                                                                                                                | ADR-021 Review 裁决入口                                                                  | 对账临时请求 schema；伪造或残留旧 head/at/path 字段被拒，Review 失效或 Issue 无效时业务记录不变                                                                                                                                             |
| 28  | Builder/Validator 自然完成时，本次 operation 内启动的服务必须同步关闭；用户在 operation 外预先启动的外部服务可复用且不得被误杀。禁止 `nohup`、daemonize 或跨轮后台逃逸；POSIX 不透明 Runner 外部终止后若无法证明内部命令域清空，必须保留永久隔离，不能用外层进程组为空宣称零残留                                                                                                                                               | ADR-021 子进程边界                                                                       | 分别运行内部临时服务与外部预启动服务：自然完成后前者零残留，后者仍存活；另让 Runner 内部命令脱离外层进程组后触发外部终止，确认 workspace 永久隔离、下一轮未启动，且报告不声称未知进程已经退出                                               |
| 29  | 父进程崩溃、终止失败或未提交 mutation 不进行 pid stale 自动接管；新 writer 只能在显式 `workspace recover` 或 `resume-mutation` 完成精确恢复后进入                                                                                                                                                                                                                                                                              | ADR-021 崩溃恢复                                                                         | 制造 hard crash 与中断 mutation；重启 run/apply 必须拒绝，错误恢复身份也拒绝，精确恢复完成后才回到 ready                                                                                                                                    |
| 30  | Developer 在开发目录实现；本地准备、契约检查、TDD、Validator 和最终 Review 前机械检查只读取项目外的精确 HEAD 检出。开发目录 `.env`、`.claude`、旧依赖和 ignored 源码不可见；回执绑定验证环境，submodule/LFS/filter、tracked 改写或越界产物都不得假绿                                                                                                                                                                           | ADR-022 精确提交干净验证                                                                 | 在真实运行前向开发目录放入同名 ignored 干扰文件，对账 Developer cwd、Validator cwd/环境、当前 receipt 环境摘要和临时目录清理；再分别制造准备失败与越界变更，确认不签发凭证                                                                 |
| 31  | Builder 只在 `progress.md` 末尾追加；历史 `## Codebase Patterns` 只读；可复用经验写入当轮“未来迭代的学习”；项目级沉淀由 `/compound-docs` 处理                                                                                                                                                                                                                                                                                  | 2026-08-05 Builder 追加指令冲突                                                          | 运行前保存 `progress.md` 前缀；运行后逐字确认此前字节不变且只新增本轮块，并确认没有新增或改写 `## Codebase Patterns`                                                                                                                        |
| 32  | Validator 启动前引擎完成宿主隔离：codex 走固定审计版本 profile + 宿主注入隔离参数/环境事实核对（`CODEX_HOME`/`HOME`/`XDG` 重定向到临时域、临时域除预置认证外为空、`--ignore-*`/`--disable` 全集）+ 真实 canary 反测（越界**写**被拒、受控项目检查与结构化回执可完成、预置凭据不外泄、进程与域收口）。诚实边界：Codex `workspace-write` 全盘可读，不声称读隔离。当前凭证的 Runner profile/canary 双摘要与 evidence `validatorProfile` 互绑且记录 canary 耗时；claude/cursor 运行启动即提示 ADR-025，进入验证阶段按不可验证保留候选并退出 5，无宽权限回退；单次调用临时身份域收口零残留，workspace 内无 validation-result.json | ADR-025 / #174 宿主隔离；2026-08-13 真机闭环实证 | 对账启动提示、evidence `validatorProfile`（resolution=ready/摘要/canaryDurationMs）、state 当前凭证双摘要、临时域与 workspace 文件系统残留；污染临时域（放非 auth 文件）或去隔离参数确认 host-context-unverifiable；再分别用 claude/cursor 与未审计版本 codex 各跑一轮确认退出 5 且候选保留 |
| 33  | 每个 Story 在首次 Developer 前冻结独立起点，后续重试不得改用 `HEAD^`；request/result/凭证绑定起点、终点与整段变化摘要。验证检出必须按质量契约建立冻结的 `origin/<defaultBranch>` 引用并把名称/目标绑定环境身份；旧候选缺起点、引用缺失/漂移、起点不可达或摘要错绑都不得签发凭证                                                                                                                                                                  | ADR-026 / #210                                                                           | 两次提交与删除后重加场景核对完整范围和末次提交范围不同；多 Story 核对各自起点；在来源仓无 origin ref 时确认检出可运行依赖该 ref 的命令，再改写别名确认失败；删除本地默认分支 ref 后确认任何 Agent 零调用且 state 不创建                                                       |
| 34  | 候选 Dogfood 必须由同一份 `packed.json` 驱动 doctor、apply-prd 和 run；实际安装文件树、Story 凭证、最终 Review、PR head/base、必需检查与发布给 staging 的证明始终绑定同一候选身份。三仓缺一、证明来自非 owner、PR 漂移、检查重跑失败或候选错绑都不能进入 npm 批准；人工批准后还要重新读取三仓并确认集合未变                                                                                                                           | ADR-027 候选身份闭环                                                                     | 在三个固定仓库分别用候选 CLI 完整运行并发布证明评论；stage 在批准前后两次机器回读三份评论与当前检查，对账候选摘要、仓库、PR head/base、凭证和 Review；再分别制造少仓、错包、错 head/base、失败的新检查、重复 marker 和批准期间变化，确认在 npm 身份前拒绝 |
| 35  | 同一次运行刚完成的全量检查以精确输入证明交给 Validator 和最终 Review：Validator 对清单明确覆盖的机械 AC 只引用证明且不重跑，语义与未覆盖能力仍独立验证；最终 Review 只有全部输入逐项相同时复用。head、默认分支基线、契约、平台、额外引用或验证环境任一变化都必须重跑，局部或失败结果永不复用                                                                                       | ADR-028 精确复用                                                                         | 用断网 Python wheel 场景确认门禁只执行一次、Validator 仍完成语义 claim；对账最终 Review 不重复。分别改变每类绑定输入确认重跑；显式加入清单不存在的 typecheck AC 确认仍不可验证；确认复用只存在于当前进程且没有 workspace 缓存                                           |
| 36  | 一个 `ready-for-agent` Issue 只产生一个固定分支和一个草稿 PR；切分支和建 PR 前即建立预备评论，首次推送/建 PR、工作区准备、等待或收口失败都更新同一条评论，再次执行只继续原运行。已有正式 Review 的全部输入仍当前时只受管刷新远端，不调用 Builder、Validator、项目检查或 Reviewer；标记可信前再执行一次同样的完整当前性观察，任一次缺失或竞态都保持未就绪。引擎结束后必须回读 PR 并确认仍开放、目标和 head 未变；可信完成只把 PR 标为 ready，不自动轮询、排队、合并或发布；总耗时始终从最新 ready 标签事件计算且不会因继续运行重置 | ADR-029 Issue 最小入口 | 同一 Issue 首次建 PR 故意失败后重试，再停在远端等待；添加审批标签后继续，断言四类完整执行调用为零、两次受管刷新并累计耗时，最终可信完成。另制造首次刷新后、最终确认前的上下文失效以及 head/base/PR 意图/规则漂移、刷新期间标签变化、工作区准备失败和引擎期间关闭 PR，确认旧 Review 不复用且都写回同一评论；对账分支/PR/评论数量、当前 head 证据、总时间/运行时间/等待时间，并确认没有 merge、release 或后台轮询调用 |
| 37  | Shadow Story 与本地 Final Review 已通过但远端检查稍晚 ready 时，`candidate publish-proof --candidate-evidence` 只复核同一候选、已有凭证/Review/Runner/PR 当前性并刷新远端，ready 后补签和发布证明；不得重跑 Builder、Validator、完整质量检查或 Review。pending/failed、错候选或任一观察竞态保持零写入、零评论                                                                                  | ADR-027 晚到远端结果恢复                                                                  | 先让 Final Review 保存 pending，再使同一 head 的远端检查 ready 并计时执行补签；对账候选摘要、最新 checkedAt、证明评论和调用边界。分别改变候选、Story 集合、head/base、契约、Runner 和远端结论，确认旧证明不被覆盖且没有发布调用                 |
| 38  | GitHub 因权限可见性省略 Ruleset 详情的 `bypass_actors` 时，普通状态检查和受管 Review 只能用稳定的最新 history 完整 state 补证；当前详情、前后最新版本标识、历史版本和规则核心必须全部一致。缺字段、版本竞态或任一差异保持不可验证，绝不猜测无人绕过                                                                                                                             | 2026-08-17 0.37.0 候选 PR #261 真实远端形状                                                | 用真实省略字段响应确认 doctor 恢复 ready；同步/受管客户端分别固定调用序列。再注入历史版本变化、当前详情变化、规则/条件差异和历史缺绕过列表，确认全部失败关闭                                                                                 |
| 39  | 含多次受管子进程、GitHub 失败重试、干净检出和安全收口的真实集成测试必须有与步骤上界匹配的单用例预算；前一用例超时后不得让仍在运行的异步流程污染紧邻用例。预算只给测试调度余量，不改变产品超时、退出码、次数或清理断言                                                                                                                                                | 2026-08-18 0.37.0 候选 PR #264 macOS 22/24 假红                                            | authority snapshot 重试用例与 clean validation 主流程/相邻清理用例连续成对运行；确认每轮自然结束、退出码稳定、无后台重叠和临时检出残留，再跑两文件完整回归与 macOS 22/24 矩阵                                                             |
| 40  | 质量检查按固定 Story 起点到当前 head 的完整 Git 变化与契约 `paths` 选择；无路径声明始终执行，未知路径、选择异常或零项回退本平台全量。复用证明额外绑定变化摘要和实际检查集合。必需 Quality Gate 在每个 PR 始终报告，只有内部 job/step 可按计划跳过；每周与人工触发始终完整矩阵。CodeQL 每个 PR 必跑；只有已分析且代码树未变的 Markdown/`docs/**` main push 跳过第二次扫描，混合 push 与 schedule/dispatch 仍完整运行 | ADR-030；PR #270 缺 CodeQL 阻塞；Issue #294 重复扫描优化 | 分别提交普通文档、源码、未分类根文件和多 Story 变化：对账本地 gate/Validator/最终 Review 的选择模式、执行与跳过 ID；聚合任务拒绝适用 job 的 skipped/失败/取消/超时。纯文档归档 PR 必须有 PR CodeQL 且可合并，合并后无 Quality Gate/CodeQL；混合 PR/main、schedule/dispatch 继续运行完整扫描 |
| 41  | POSIX fixed supervisor 已原子安装严格绑定的 canonical receipt、但 parent 未收到 DRAINED IPC 引用时，当前 owner 可从同一 receipt 重建引用并继续原 ACK/精确死亡/空集合/settlement；receipt 缺少的 RESULT 不得推断，安全结算后本次调用仍失败。缺失、篡改、跨 operation 或最终事实不完整继续隔离                                                                 | ADR-021 / Issue #291；Issue #288 两次真实长尾                                            | 确定性丢弃 DRAINED，确认数字退出码与正常结论保留且在 ACK 预算内结束；再同时丢弃 RESULT，确认 operation 已结算、workspace 可关闭但调用不可验证。篡改 operationId/摘要必须拒绝；连续运行真实短命令确认零 active operation、零残留进程组且不等待业务完整超时 |
| 42  | 相同输入的 Issue 续跑用一个固定受管 preflight snapshot 批量收集 branch、base/head、PR、契约、完整 diff、逐文件 base/head、Spec、工程规范和 history 原始事实；父引擎继续用原 parser/risk/binding 裁决。快照执行/解析不可用时安全回退，返回事实与契约矛盾时失败关闭；远端慢读取前后和可信标记前的独立确认均保留，不跨请求缓存                                                           | ADR-029 / Issue #292                                                                    | 用同一真实仓库对账 snapshot context 与旧 preflight 逐字段相等且外层 operation=1；注入二进制、LFS、submodule、脏树、错文件/来源路径和输出超界确认拒绝或回退。对同一 PR 连续实跑三次单确认和一次双确认，记录耗时与 operation 数，确认零 Builder/Validator/项目检查/Reviewer 调用 |
