__pycache__/
*.pyc
.venv/
dist/
.idea/
.vscode/
/.coverage
coverage.xml
diff-cover.html
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 环境配置（含密钥，禁止提交）
.env
conf/secrets/

# 日志目录
logs/

# OS
.DS_Store
Thumbs.db

# Claude Code 本地运行/个人目录（settings.local.json、worktrees 等，均不共享）
# 注意：插件的可共享配置在 .claude-plugin/，与此目录无关。
# 若将来需要提交共享配置，可加负向规则，例如 !.claude/settings.json
.claude/

# superpowers 回退的 git worktree 隔离空间（仓库根）
.worktrees/

# Node / coding-x
node_modules/
.workspace/
