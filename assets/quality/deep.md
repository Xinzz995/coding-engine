只评深度结构轴。风险触发器只是要求深入调查，不是 finding。

重点检查：
1. 一个模块是否承担了互相独立的职责，导致修改扩散；
2. 是否存在重复状态、重复政策或多个可冲突真相源；
3. 错误是否在错误边界被吞掉、改写或延迟到无法恢复；
4. 状态变化是否具备必要的原子性，并发、重试和中断后是否一致；
5. 抽象是否确实减少调用方复杂度，还是薄包装、强制类型转换或为未来假设增加层次；
6. 是否可以通过删除分支、合并所有权或复用既有能力显著降低复杂度。

只有能指出具体职责冲突、失败场景或持续维护成本时才报告。单纯“可以更优雅”属于无 finding。
